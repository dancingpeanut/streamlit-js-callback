import React from "react"
import ReactDOM from "react-dom"
import JSCallbackComponent from "./JSCallbackComponent"

ReactDOM.render(
  <React.StrictMode>
    <JSCallbackComponent />
  </React.StrictMode>,
  document.getElementById("root")
)
